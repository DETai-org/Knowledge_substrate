# Redmine

## Лог изменений (локальная фиксация)

- Добавлены `config.json` для пайплайна embeddings/графа и переменные окружения в `detai-core/.env.example`.
- Пайплайн обновлён для чтения конфигурации и приоритета CLI → config → env → defaults.
