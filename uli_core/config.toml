# Базовая директория, где лежат служебные данные UliCore.
# 👉 То есть, при копировании uli_core в новый проект —
# тебе достаточно заменить только base_dir с /projects/ на название той папки в которую добовляешь uli_core

base_dir = "/srv/Knowledge_substrate/uli_core"

# Путь к TODO-файлу репозитория (для синхронизации задач).
todo_file = "TODO.md"

# Основной JSON с данными центра синхронизации.
issue_file = "Issues/issue_1.json"
