# 🧩 UliCore

**UliCore** — переносимый слой инфраструктуры для каждого репозитория в github.com/DETai-org 
Он обеспечивает обработку входящих событий GitHub, синхронизацию данных и единый формат внутренней конфигурации.

Но им управляет Uli Center
О нем [См. Uli Center/README.md](../uli/center/README.md)

## 🌐 Назначение

UliCore отвечает за:
- синхронизацию с сервером;
- синхронизацию `TODO.md` репозитория с актуальными задачами (`issues`) GitHub;
- обработку входящих `payload` от централизованного сервера (`uli/center`).
---

## 🧩 Концепция переносимого ядра

Каждый проект, участвующий в экосистеме DET, содержит копию папки `uli_core/`.  
Это позволяет:
- использовать общий синхронизатор и конфигурационный интерфейс;
- обеспечивать совместимость между проектами при любых обновлениях;
- запускать обработку данных без привязки к конкретному серверу или окружению.

Папка `uli_core/` включает базовые настройки:
```
uli_core/
├── config.toml       # Конфигурация путей и базовых файлов
├── config.py         # Интерфейс для чтения TOML и внутренних настроек
└── Directory_structure.txt
```

## 🌍 Централизованная синхронизация

UliCore не взаимодействует с GitHub напрямую.
Webhook на уровне **организации GitHub** передаёт события в централизованный сервер `uli/center`.
Центр:

1. принимает webhook;
2. делает `git pull` нужного репозитория;
3. вызывает обработчик `uli/center/scripts/collect_github_meta.py` внутри проекта.

Таким образом, каждый проект получает обновления автоматически,
а `uli_core` выполняет только локальную обработку и синхронизацию данных.

---

### ✅🆕 Чек-лист для создания нового проекта

1. **Создание нового репозитория на GitHub:**

* В GitHub в разделе Repositories создай новый репозиторий с именем `{ProjectName}`, не добавляя начальных файлов (README.md, .gitignore и т.д.).

2. **Создание новой папки для проекта на сервере:**

* Перейди в корневую директорию пользователя:
```bash
cd /home/psy-ru/
```

```bash
mkdir {ProjectName}
cd {ProjectName}
```

3. **Инициализация репозитория на сервере:**

```bash
git init
```

* Добавь репозиторий на GitHub:
```bash
git remote add origin git@github.com:DETai-org/{ProjectName}.git
```

4. **Копирование папки `uli_core/:**

скопируй в новый проект:
```bash
cp -r /home/psy-ru/projects/uli_core /home/psy-ru/{ProjectName}/
```


5. **Настройка `config.toml` для нового проекта:**

* Открой файл `config.toml` в папке `uli_core` и настрой параметр `base_dir`:

```toml
base_dir = "/home/psy-ru/{ProjectName}/uli_core"
```

5. **Настройка ` Главный config.toml` для нового проекта:**

В [Главном config.toml](../config.toml) добавь новый раздел



7. **Настройка данных пользователя для Git (если ещё не настроено):**

```bash
git config --global user.name "psy-ru"
git config --global user.email "kolhonena@gmail.com"
```


8. **Создание ветки `main` и пуш первого коммита:**

```bash
git branch -M main
```

* Добавь файлы в индекс Git и сделай первый коммит:

```bash
git add .
git commit -m "Initial commit with uli_core"
```

* Запушь изменения на GitHub:

```bash
git push -u origin main
```

8. **Проверка синхронизации:**

* Сделай тестовое изменение в проекте на GitHub (например, обнови README) и запушь его:

____
© DETai Infrastructure — модульная система синхронизации и автоматизации.
