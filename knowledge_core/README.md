# DET / DETai — Knowledge Core

Внутренняя каноническая база знаний экосистемы **DET / DETai**. Репозиторий хранит исходные Markdown-материалы и публикует их через **MkDocs Material** на **GitHub Pages**.

## Что внутри 🦾

- **knowledge_core/source_of_truth/docs/** — единый канон знаний:
  - **ecosystem/** — экосистема (стандарты, продукты, концепты)
  - **publications/** — домен публикаций (структура и метаданные)
- **knowledge_core/source_of_truth/controlled_vocabularies/** — канонические словари значений
- **knowledge_core/source_of_truth/schemas/** — контракты доменов
- **mkdocs.yml** — конфигурация сайта
- **.github/workflows/docs.yml** — автосборка и деплой

## Быстрый старт (локально)

> Требуется Python 3.11+

```bash
pip install mkdocs-material
mkdocs serve
```

После запуска документация будет доступна по адресу `http://127.0.0.1:8000/`.

## Деплой

Деплой выполняется автоматически при пуше в ветку `main`, если изменялись:
- `knowledge_core/source_of_truth/docs/**`
- `mkdocs.yml`
- `.github/workflows/docs.yml`

## Автопроверка

```bash
./scripts/check.sh
```

Скрипт запускает:
- `mkdocs build --clean`
- `python -m compileall detai-core`

## Принципы ведения

- **Markdown — канон знаний**: изменения обсуждаются и фиксируются в PR.
- **Структура важнее наполнения**: каркас должен быть стабилен.
- **RU + EN**: минимальные англоязычные страницы поддерживаются для навигации.

## Как публикуется MkDocs

- Навигация больше **не задаётся вручную** — она автогенерируется из структуры файлов.
- Для раздела публикаций включены **исключения `exclude_docs`**.
- Публикации остаются в Source of Truth, но **не публикуются в MkDocs** (кроме `docs/publications/index.md`).

Пример фрагмента `mkdocs.yml`:

```yaml
plugins:
  - search
  - awesome-pages

exclude_docs:
  - publications/Research_Publication/**
  - publications/STORYTELLING/**
  - publications/blogs/**
```

## Как добавлять материалы

1. Добавьте или обновите Markdown-файл в `knowledge_core/source_of_truth/docs/`.
2. При необходимости настройте `.pages` в нужной папке, чтобы управлять заголовками и порядком.
3. Откройте PR с кратким описанием изменений.

---

Если нужно расширить структуру, сначала согласуйте изменения в структуре каталога.
