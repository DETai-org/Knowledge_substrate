# Source of Truth (SoT)

**Source of Truth** экосистемы **DET / DETai** включает **канонические Markdown-документы**, их **метаданные** (frontmatter) и **Controlled vocabulary** (канонические реестры допустимых значений, например: rubrics, categories, keywords).

## Структура

- `docs/` — единый канон контента и источников знаний.
  - `docs/publications/` — домен публикаций: посты, статьи, исследования.
  - `docs/ecosystem/` — домен экосистемы: стандарты, продукты, концепты.
- `controlled_vocabularies/` — канонические словари значений.
- `schemas/` — контракты структуры данных.

## Правило

`docs/` остаётся **единым каноном контента** (и для MkDocs, и для ingest), а домены — подпапки внутри него.

## Публикация — это фильтр, а не обязанность

Не все документы из Source of Truth обязаны быть опубликованы в MkDocs.

Публикация управляется **исключительно** через файл конфигурации `mkdocs.yml`, а именно:
- через структуру `nav`;
- через исключение отдельных файлов или директорий.

Следовательно:
- документ может **существовать в каноне** и использоваться системой;
- но **не отображаться** в MkDocs и не быть доступным публично.

---

## Особое правило для публикаций (publications_schema)

Документы, относящиеся к домену **публикаций** (`publications_schema`),
**НЕ публикуются в MkDocs как контент**.

Причина:
- публикации (посты, статьи, цитаты) имеют собственный публичный канал (основной сайт);
- MkDocs используется как документация и навигация по знанию, а не как блог.

При этом:
- структура `docs/publications/` может **присутствовать в MkDocs навигации**;
- но страницы внутри неё **не раскрывают сами публикации**.

---

## Как публикации представлены в MkDocs

В MkDocs для раздела публикаций допускается и рекомендуется:

- оставить раздел/страницу-указатель (stub page);
- разместить поясняющий текст, например:

> **Наши публикации доступны на основном сайте.**
> Здесь хранится каноническая структура и метаданные публикаций.
> 👉 [Перейти к публикациям на сайте](https://example.com)

Таким образом:
- MkDocs сохраняет **целостную карту знания**;
- пользователь понимает, *где* живут публикации;
- при этом не происходит дублирования контента между сайтом и документацией.

---

Эти материалы являются **первичным источником знания** и используются как единый фундамент для:

- **публикации** через **MkDocs Material** (публикуется только то, что явно включено в `mkdocs.yml`);
- **индексации** и структурирования данных в операционном хранилище (SQL/Cloud SQL);
- **построения knowledge graph** (связи, ссылки, подграфы по доменам);
- **работы интеллектуальных агентов** (поиск, RAG, embeddings и другие машинные представления).

**Controlled vocabulary** живут **внутри Source of Truth**, но **не являются частью конкретных документов**: это отдельные канонические реестры, на которые документы ссылаются через стабильные ID.

Базы данных и сервисы (ingest pipeline, API, Postgres) являются **производными проекциями** этого канона: при необходимости они могут быть **воспроизведены заново** из Source of Truth.
