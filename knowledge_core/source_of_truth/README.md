# Source of Truth (SoT)

**Source of Truth** экосистемы **DET / DETai** включает:
**канонические Markdown-документы**, их **метаданные** (frontmatter),
**Controlled vocabulary** (канонические реестры допустимых значений, например: rubrics, categories, keywords).

## Структура

Source of Truth — это **единая база знаний**, которая раскладывается по двум главным векторам:

1. **Контентный вектор (`docs/`)** — канон документов и источников знаний.  
   Эта же папка одновременно выступает как база данных и как **витрина MkDocs GitHub**.
   - `docs/{lang}/ecosystem/` — домен экосистемы: стандарты, продукты, концепты.
   - `docs/publications/` — домен публикаций: посты, статьи, исследования.

2. **Контрактный вектор (`schemas/`)** — схемы как договорённости о том,
   **как фиксируется, именуется, нумеруется и связывается информация**.
   Это «правила сборки» знаний, которые определяют формат и структуру данных.

Коротко о зеркальности доменов: и в `docs/`, и в `schemas/` выделены направления
`ecosystem` и `publications`. При этом в MkDocs публикуется только экосистема
(в нескольких языках), а публикации остаются внутри канона и не выходят в публичную витрину.

## Policies

Политики — это канонические нормативные документы, которые закрепляют принципы устройства,
роста и управления знаниями в Knowledge Core. Они задают правила для словарей, таксономии,
структуры метаданных и безопасной индексации, чтобы Source of Truth оставался согласованным
с производными системами (индекс, RAG, граф). В результате любые изменения в данных и схемах
сверяются с этими правилами, а не обсуждаются «с нуля».

Где искать политики:
- Общие правила схем и словарей — `knowledge_core/source_of_truth/schemas/README.md`.
- Политики публикаций — `knowledge_core/source_of_truth/schemas/publications/post_documents/README.md`.
- Ingest & RAG hybrid model — `knowledge_core/ingest_pipeline/policies/ingest_and_rag_hybrid_model.md`.

## Правило

Материалы тут являются **первичным источником знания** и используются как единый фундамент для:

- **публикации** через **MkDocs Material** (публикуется только то, что явно включено в `mkdocs.yml`);
- **индексации** и структурирования данных в операционном хранилище (SQL/Cloud SQL);
- **построения knowledge graph** (связи, ссылки, подграфы по доменам);
- **работы интеллектуальных агентов** (поиск, RAG, embeddings и другие машинные представления).

**Controlled vocabulary** живут **внутри Source of Truth**, но **не являются частью конкретных документов**:
это отдельные канонические реестры, на которые документы ссылаются через стабильные ID.

Базы данных и сервисы (ingest pipeline, API, Postgres) являются **производными проекциями** этого канона: при необходимости они могут быть **воспроизведены заново** из Source of Truth..


---

# MkDocs GitHub 🐱 - Как публикации представлены в MkDocs

- навигация MkDocs **автогенерируется из структуры файлов**;
- **все изменения в `docs/ecosystem/` автоматически публикуются**;
- **из `docs/publications/` публикуется только `index.md`**;
- подпапки `Research_Publication`, `STORYTELLING`, `blogs` исключены из публикации.
  
Таким образом:
- MkDocs сохраняет **целостную карту знания**;
- пользователь понимает, *где* живут публикации;
- при этом не происходит дублирования контента между сайтом и документацией.

### Публикация — это фильтр, а не обязанность

Не все документы из Source of Truth обязаны быть опубликованы в MkDocs.

Публикация управляется **исключительно** через файл конфигурации `mkdocs.yml`, а именно:
- через автогенерацию навигации из структуры файлов;
- через исключение отдельных файлов или директорий (`exclude_docs`).

Следовательно:
- документ может **существовать в каноне** и использоваться системой;
- но **не отображаться** в MkDocs и не быть доступным публично.
---
