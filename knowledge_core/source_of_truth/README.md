# Source of Truth (SoT)

**Source of Truth** экосистемы **DET / DETai** включает **канонические Markdown-документы**, их **метаданные** (frontmatter) и **Controlled vocabulary** (канонические реестры допустимых значений, например: rubrics, categories, keywords). 

### Правило ©️

* `docs/` остаётся **ЕДИНЫМ каноном контента** (и для MkDocs, и для ingest)
* внутри `docs/` появляются **два домена** (две зоны ответственности):

  1. `publications/` (контент-публикации)
  2. `ecosystem/` (стандарты/продукты/концепты экосистемы)

То есть `docs/` — общий “корень знания”, а домены — подпапки.


Эти материалы являются **первичным источником знания** и используются как единый фундамент для:

- **публикации** через **MkDocs Material** (при этом публикуется только то, что мы явно включаем в навигацию/структуру через файл `mkdocs.yml`);
- **индексации** и структурирования данных в операционном хранилище (SQL/Cloud SQL);
- **построения knowledge graph** (связи, ссылки, подграфы по доменам);
- **работы интеллектуальных агентов** (поиск, RAG, embeddings и другие машинные представления).

**Controlled vocabulary** живут **внутри Source of Truth**, но **не являются частью конкретных документов**: это отдельные канонические реестры, на которые документы ссылаются через стабильные ID.  

Базы данных и сервисы (ingest pipeline, API, Postgres) являются **производными проекциями** этого канона: при необходимости они могут быть **воспроизведены заново** из Source of Truth.

## Правила публикации и использования Source of Truth

### 1. Две логики — один канон

**Source of Truth** обслуживает **две логики одновременно**:

1. **Канон данных**  
   Все Markdown-документы и их метаданные используются как первичный источник данных для:
   - ingest pipeline,
   - SQL-базы (operational store),
   - knowledge graph,
   - интеллектуальных агентов.

2. **Публикация документации**  
   Те же самые Markdown-документы могут использоваться для публикации через **MkDocs Material** — как человекочитаемое представление части канона.

При этом **Source of Truth остаётся единым**: никаких копий, дублирования или “вторых версий” документов не существует.

---

### 2. Публикация — это фильтр, а не обязанность

Не все документы из Source of Truth обязаны быть опубликованы в MkDocs.

Публикация управляется **исключительно** через файл конфигурации `mkdocs.yml`, а именно:
- через структуру `nav`;
- через исключение отдельных файлов или директорий.

Следовательно:
- документ может **существовать в каноне** и использоваться системой;
- но **не отображаться** в MkDocs и не быть доступным публично.

---

### 3. Особое правило для публикаций (publications_schema)

Документы, относящиеся к домену **публикаций** (`publications_schema`),  
**НЕ публикуются в MkDocs как контент**.

Причина:
- публикации (посты, статьи, цитаты) имеют собственный публичный канал (основной сайт);
- MkDocs используется как документация и навигация по знанию, а не как блог.

При этом:
- структура `docs/publications/` может **присутствовать в MkDocs навигации**;
- но страницы внутри неё **не раскрывают сами публикации**.

---

### 4. Как публикации представлены в MkDocs

В MkDocs для раздела публикаций допускается и рекомендуется:

- оставить раздел/страницу-указатель (stub page);
- разместить поясняющий текст, например:

> **Наши публикации доступны на основном сайте.**  
> Здесь хранится каноническая структура и метаданные публикаций.  
> 👉 [Перейти к публикациям на сайте](https://example.com)

Таким образом:
- MkDocs сохраняет **целостную карту знания**;
- пользователь понимает, *где* живут публикации;
- при этом не происходит дублирования контента между сайтом и документацией.

---

### 5. Метаданные и Controlled Vocabulary

Метаданные документов (frontmatter), включая:
- `rubric_ids`,
- `category_ids`,
- `status`,
- `date_ym`,
и ссылки на **Controlled vocabulary**:

- являются **частью канона**;
- используются ingest pipeline и базой данных;
- **не обязаны** отображаться в MkDocs при публикации.

Отображение или скрытие метаданных — вопрос темы/конфигурации MkDocs и **не влияет на статус документа как канонического**.

---

### 6. Controlled Vocabulary как часть Source of Truth

**Controlled vocabulary** (rubrics, categories, keywords и др.):

- живут **внутри Source of Truth**;
- существуют как отдельные канонические реестры;
- **не являются частью конкретных документов**, но используются ими через ID.

Они:
- не публикуются как контент;
- могут быть использованы для документации (описания, справочники);
- служат единым источником истины для данных и логики системы.

---

### 7. Ключевой принцип

> **Source of Truth — это знание.  
> MkDocs — это одно из его представлений.**

Базы данных, сайты, API и агенты — все они являются **производными проекциями** канона и могут быть полностью восстановлены из Source of Truth.
