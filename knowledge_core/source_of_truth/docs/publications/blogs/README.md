# blogs

Все материалы, попадающие в одну из этих папок, автоматически копируются в

📚 Временные хранилища блог‑постов в репо sites **[DETai-org/sites → docs/publications/blogs](https://github.com/DETai-org/sites/tree/main/docs/publications/blogs)**.

Таким образом, база знаний выступает **источником истины** для блоговых материалов, а сайт — их рабочей витриной.  
Копирование выполняется автоматически системой синхронизации **[Sync Blog Posts to Site](https://github.com/DETai-org/Knowledge_substrate/blob/main/.github/workflows/sync-blog-posts.yml)** и не требует ручного переноса файлов.

## Список обработанных публикаций

В этой папке хранится файл `list_publication_blogs.json`. Он используется workflow `Create Linear issue on merge to main` как реестр уже обработанных публикаций: если файл поста есть в списке, задача в Linear не создаётся повторно. После успешного создания задачи имя файла добавляется в этот JSON автоматически.

| Сведения | detai_site_blog | personal_site_blog |
| --- | --- | --- |
| Кол-во постов 🔢 | 31 | 44 |
