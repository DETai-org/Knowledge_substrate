## 🎯 Назначение

Этот документ — **единый источник правил** для генерации frontmatter постов типа `post`.

- Этот файл **не дублирует** сложные правила: он даёт мини-инструкции и **ссылки** на policy/json в этой же папке.

## Контекст и источники истины

Перед работой сверяйся с каноном:

- **Schema и правила постов:**
  - `README.md`

- **Controlled vocabularies:**
  - `rubrics.json`
  - `categories.json`
  - `keywords.json`
  - `cycle.json`
  - `sizes.json`
  - `../authors.json`

- **Политики:**
  - `rubrics_and_subrubrics_policy.md`
  - `category_policies.md`
  - `keywords_policy.md`
  - `preview_policies.md`
  - `cycle_policies.md`
  - `size_policies.md`

## 📃 Создание файла

Файл поста создаётся в:

```
knowledge_core/source_of_truth/docs/publications/blogs/<TARGET_SITE>/
```

Где `<TARGET_SITE>` определяется входными данными (`detai_site_blog` или `personal_site_blog`).

Пользователь может описать это не точно, например на русском языке: «персональный сайт», «DETai сайт» и т.д.
Ты должен сам понять, к какому из двух сайтов это относится.

Пользователь может указать **несколько** сайтов, например через запятую, в свободной форме и на русском (например: «detai», «ДЕТай», «персональный сайт»). В этом случае **нужно сохранить один и тот же файл** в **каждой** соответствующей папке:

```
knowledge_core/source_of_truth/docs/publications/blogs/detai_site_blog/
knowledge_core/source_of_truth/docs/publications/blogs/personal_site_blog/
```

Если указано несколько сайтов и пользователь **не задал рубрику**, выбирай рубрику, которая **допустима для обоих сайтов** (общая рубрика). Список рубрик и правила см. в `rubrics.json` и `rubrics_and_subrubrics_policy.md`.

Если пользователь оставляет поле пустым (например authors: без значения), это означает, что данные не переданы. В таком случае не подставлять это поле как фактическое значение, а действовать по правилам

Название файла должно быть равно `<postId>.md`, где `postId` создаётся на основе заголовка.

**Пример:**

- `title`: Эта история про одного человека
- `postId`: eta-istoriya-pro-odnogo-cheloveka
- **Название файла:** `eta-istoriya-pro-odnogo-cheloveka.md`

## 🧩 Taxonomy (правила заполнения)

### `taxonomy.rubric_ids`

- Если `rubric_ids` **заданы пользователем**:
  - если переданы как ID → использовать после валидации по `rubrics.json`;
  - если переданы как название (title) → сопоставить с допустимыми рубриками, допуская орфографические ошибки и вариации написания; выбрать наиболее вероятное совпадение.
- Если `rubric_ids` **не заданы** → определить рубрику по смыслу текста.
- Подробнее: `rubrics_and_subrubrics_policy.md` + `rubrics.json`.

### `taxonomy.category_ids`

**Правило выбора (приоритет):**

1. Если в тексте есть эмодзи, совпадающая с теми, что в `categories.json` → выбрать эту категорию.
2. Если эмодзи несколько → выбрать **наиболее релевантную по смыслу**.
3. Если эмодзи нет → выбрать категорию **по смыслу текста**.

Подробнее: `category_policies.md` + `categories.json`.

### `taxonomy.cycle_ids` (**0 или 1 элемент**)

- Если пользователь передал:
  - `cycle_id`, **или** название цикла (title), → сопоставить с `cycle.json`, допуская орфографические ошибки и вариации написания; выбрать наиболее вероятное совпадение по смыслу.

Если пользователь **не указывает** цикл → `cycle_ids` **не добавлять**.

Пользователь может указать цикл **в свободной форме**, включая:

- название цикла;
- текстовое описание намерения (например: что это первый пост нового цикла).

Если из значения поля логически следует, что текущий пост является **первым постом нового цикла**, и соответствующий цикл отсутствует в `cycle.json`, то:

- должен быть создан новый цикл.

Подробнее: `cycle_policies.md` + `cycle.json`. Если цикл размещается на **двух сайтах**, см. правила в `cycle_policies.md`.

### `taxonomy.keyword_ids` + `taxonomy.keywords_raw`

- `keywords_raw`:
  - сгенерировать список **сырых** ключевых слов или словосочетаний (2–3) по смыслу текста.
- `keyword_ids`:
  - проанализировать текст и определить, **какие ключевые слова подходят по смыслу**;
  - сопоставить их с допустимыми ID из `keywords.json` (с учётом политики);
  - подставить **только те**, которые прошли сопоставление.
- Если подходящих нет → `keyword_ids` **не добавлять**.
- Подробнее: `keywords_policy.md` + `keywords.json`.

### `authors`

- Список авторов берётся из `knowledge_core/source_of_truth/schemas/publications/authors.json`.
- Если пользователь указывает автора в свободной форме (включая только имя без фамилии, в том числе на русском — например, «Антон»), сопоставь значение с `authors.json` и подставь каноническое имя автора из файла, если есть хотя бы частичное совпадение.
- Если `authors` отсутствует → указать `authors: [none]` (не подставлять автоматически реального автора).

### `preview`

- Сгенерировать краткий лид: 1–2 предложения, без списков и без ссылок.
- Подробнее: см. `preview_policies.md`.

### `descriptive.size`

- Значение берётся из `sizes.json`.
- Выбор размера — по масштабу и структуре текста (см. `size_policies.md`).
- Если пользователь **не указал размер**, выбрать его самостоятельно по этим правилам.

### SEO (`seoLead`)

**Обязательное поле.**
1–2 предложения (≈ 90–160 символов), без кликбейта, с ключевой темой в начале.

Требования:

- конкретно описывает содержание;
- включает 1–2 естественные ключевые фразы;
- не дублирует заголовок целиком;
- без эмодзи и капслока.

### `date_ymd`

- Если дата не передана во входе: поставить **текущую дату** в формате `YYYY-MM-DD`.

### `status`

- Всегда `publish`.

### `target_site`

- Может содержать **один или несколько** сайтов.
- Если пользователь перечисляет сайты через запятую, или использует свободные формулировки/русский язык (например «detai», «ДЕТай», «персональный сайт») — сопоставь с допустимыми значениями `detai_site_blog` и `personal_site_blog`.
- При нескольких сайтах сохраняй **один и тот же файл** в **каждой** папке `knowledge_core/source_of_truth/docs/publications/blogs/<TARGET_SITE>/`.

### `type`

- Всегда `post`.

## 📦 Выход: frontmatter (YAML) + тело поста

Frontmatter должен начинаться и заканчиваться `---`.

**Шаблон (структура фиксирована):**

```yaml
---
type: post
id: <postId>
authors:
  - <AuthorName>
date_ymd: <YYYY-MM-DD>
status: publish
target_site:
  - detai_site_blog
  - personal_site_blog
title: "<title>"
preview: "<preview>"
seoLead: "<seoLead>"
descriptive:
  size: "<M|L|XL|XXL>"
taxonomy:
  rubric_ids: ["rubric:..."]
  category_ids: ["category:..."]
  cycle_ids: ["cycle:..."]        # добавлять только если цикл есть
  keyword_ids: ["keyword:..."]    # добавлять только если есть совпадения
  keywords_raw: ["..."]           # список сырых ключевых слов
---
```
