# Cycle Policies

**v1.0** ♻️

## Назначение документа

Этот документ описывает правила работы с **thematic cycles** (тематическими циклами)
в документах типа **post** и фиксирует их роль в публикационной таксономии.

---

## 1. Определение тематического цикла

**Тематический цикл** — это последовательность публикаций,
связанных единой идеей, стилем и задачей формирования имиджа автора
в социальных сетях как психолога и психотерапевта.

---

## 2. 🎯 Цели тематических циклов

- Формировать целостное восприятие автора как специалиста.
- Превращать публикуемый контент в интеллектуальную и эмоциональную траекторию.
- Проводить читателя по смыслу, который раскрывается постепенно.
- Закреплять профессиональные или философские идеи через повторение,
  связность и глубину.
- Повышать доверие, вызывать отклик, вовлекать.

---

## 3. 🧠 Ключевая идея: цикл как мини-проект

Каждый цикл — это **мини-проект**, в котором есть:

- **структура**: начало → развитие → поворот → завершение;
- **миссия**: какая идея или позиция передаётся аудитории;
- **ритм**: частота публикаций и баланс лёгкого/сложного;
- **отклик**: запуск смыслов и отражение в сознании аудитории.

---

## 4. 🧩 Общая структура цикла

1. **Пост-анонс** (announcement):
   - первый пост нового цикла;
   - фиксирует тему, стиль, миссию и формат;
   - объясняет, что будет происходить в цикле.
2. **Основной блок**:
   - посты 2–6 (или больше);
   - раскрывают идею поэтапно и логически.

---

## 5. Таксономия и идентификаторы

### 5.1 ID цикла

- Каждый цикл имеет стабильный `id` в формате `cycle:<slug>`.
- ID фиксируется в `post_documents/cycle.json`.

### 5.2 Привязка поста к циклу

- В `taxonomy.cycle_ids` указывается **один** `id` цикла.
- Если пост не входит в цикл — `cycle_ids` не указывается.

### 5.3 Создание нового цикла

Если в `cycle.json` **нет** указанного `id` или `title`,
и при этом в данных явно присутствует намерение создать новый цикл,
то цикл **должен быть создан** в `cycle.json`.

Минимальный набор данных при создании цикла:

- `administrative.id` (обязателен);
- `descriptive.title` (обязателен);
- `descriptive.preview` (обязателен);
- `descriptive.content` (обязателен);
- `descriptive.seoLead` (рекомендуется).

---

## 6. Маркировка поста-анонса

Пост-анонс фиксируется отдельно, чтобы было видно,
что он является **родительским** для остальных постов цикла.

Для этого в `taxonomy` вводится дополнительное поле:

- `cycle_role: "announcement"` — пост-анонс;
- `cycle_role: "member"` — любой обычный пост внутри цикла.

Если поле `cycle_role` отсутствует, по умолчанию считается `member`.

---

**Статус документа:** draft
**Последнее обновление:** 2026-01-15
