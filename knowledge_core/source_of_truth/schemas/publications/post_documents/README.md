# Политика publications_schema (post_documents)

**v1.1** ♻️

## Общее описание

Этот раздел фиксирует **канонические правила для документов типа post** и их таксономии.
Здесь собраны:

- **структуры** сущностей (JSON-реестры);
- **политики** их применения;
- **мини-индекс** для быстрого обзора.

Ключевой принцип: **документы не содержат произвольные строки для таксономии** —
они ссылаются на сущности controlled vocabularies по стабильным `id`.

---

## Сущности и их контракты

Ниже перечислены основные сущности. **Структуру смотрите в JSON**, а **правила — в политике**.

### Rubrics / Subrubrics

- **JSON-реестр:** `post_documents/rubrics.json`
- **Политика:** `post_documents/rubrics_and_subrubrics_policy.md`

Rubric задаёт концептуальный вектор, subrubric появляется как результат накопления данных.

---

### Categories

- **JSON-реестр:** `post_documents/categories.json`
- **Политика:** `post_documents/category_policies.md`

Category — поперечный ракурс, уточняющий аспект рассмотрения темы.

---

### Keywords

- **JSON-реестр:** `post_documents/keywords.json`
- **Политика:** `post_documents/keywords_policy.md`

Keywords — эмпирический слой, отражающий содержание текста и поддерживающий рост rubrics.

---

### Cycles

- **JSON-реестр:** `post_documents/cycle.json`
- **Политика:** `post_documents/cycle_policies.md`

Cycles фиксируют принадлежность поста к тематическому циклу (если применимо).

---

## Мини-индекс

Для быстрого обзора сущностей используйте:
`post_documents/_indexes_min.md`.

---

## Правила применения сущностей

### Rubric

- В `taxonomy.rubric_ids` должна быть **минимум одна** rubric.
- Для `type = post` допускается **ровно одна** rubric.

### Category

- Для `type = post` требуется **ровно одна** category.
- Categories уточняют ракурс, но **не определяют смысл** документа.

### Keywords

- `keywords_raw` — допустимы как переходный слой.
- `keyword_ids` — целевое состояние после ревью и утверждения.

### Cycles

- Если пост входит в цикл, указывается **один** `cycle_id` в `cycle_ids`.
- Если пост **не входит** в цикл, поле **не указывается**.
- Для поста-анонса указывается `cycle_role = "announcement"` (см. политику cycles).

---

## Preview

Превью оформляется отдельной политикой (`post_documents/preview_policies.md`),
потому что это **самостоятельный PR-инструмент**: короткий публичный тезис,
который должен заинтересовать читателя и привести к полному тексту. 😍

---

**Статус документа:** canonical
**Последнее обновление:** 2026-01-15
